{
  "name": "quick-transpose-monorepo",
  "version": "1.0.0",
  "description": "Cross-platform chord transposition application",
  "private": true,
  "scripts": {
    "build": "npm run build:shared && npm run build:web && npm run build:miniprogram",
    "build:shared": "cd shared && npm run build",
    "build:web": "cd platforms/web && npm run build",
    "build:miniprogram": "cd platforms/miniprogram && npm run build",
    "dev": "npm run dev:shared & npm run dev:web",
    "dev:shared": "cd shared && npm run dev",
    "dev:web": "cd platforms/web && npm run dev",
    "dev:miniprogram": "echo 'Use WeChat Developer Tools to develop miniprogram'",
    "clean": "npm run clean:shared && npm run clean:web",
    "clean:shared": "cd shared && npm run clean",
    "clean:web": "cd platforms/web && npm run build && rm -rf dist",
    "install:all": "npm install && cd shared && npm install && cd ../platforms/web && npm install",
    "lint": "cd platforms/web && npm run lint",
    "test": "echo 'Tests will be added in future iterations'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/quick-transpose.git"
  },
  "keywords": [
    "chord",
    "transposition",
    "music",
    "cross-platform",
    "react",
    "miniprogram",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT"
}